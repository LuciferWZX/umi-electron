import styled from 'styled-components';
import { Layout } from 'antd';
const { Content } = Layout;
export const StyledBasicLayout = styled(Layout)`
  height: 100%;
`;
export const StyledContent = styled(Content)`
  height: 100%;
`;
